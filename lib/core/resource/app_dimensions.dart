/// A class that holds the application's dimension values.
class AppDimension {
  /// Horizontal padding applied to screens.
  final double screenHorizontalPadding = 12.0;

  /// Vertical padding applied to screens.
  final  double screenVerticalPadding = 12.0;

  /// Spacing between items in a list or grid.
  final  double itemSpacing = 12.0;

  /// Height of dividers used in lists or between sections.
  final  double dividerHeight = 1.0;

// Add more dimensions as needed
}
